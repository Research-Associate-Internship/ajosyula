name: Snyk Vulnerability Scan
on:
  push:
    branches:
      - codeql

jobs:
  snyk-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: echo "This is Install dependencies step"
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Install Snyk CLI
        run: "This is install snyk cli step"

      - name: Snyk Vulnerability Scan
        run: echo "This is Snyk Vulnerability Scan step"
        #continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
